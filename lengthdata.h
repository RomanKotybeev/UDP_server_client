#ifndef LENGTHDATA_H_ENTRY
#define LENGTHDATA_H_ENTRY
enum {
	max_msg_len = 508, /* The length of the recieved and sent message. */
	max_metadata_len = 72 /* The length of metadata mesage */
};
#endif
